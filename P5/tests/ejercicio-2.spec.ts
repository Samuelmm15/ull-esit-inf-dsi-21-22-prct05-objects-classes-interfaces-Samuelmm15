import 'mocha';
import {expect} from 'chai';
import {example} from '../src/ejercicio-2/ejercicio-2';

describe(' Connect-4 class tests', () => {
  it('example returns value WIN PLAYER ONE', () => {
    expect(example);
  }),
  it('example returns value WIN PLAYER TWO', () => {
    expect(example);
  });
});
